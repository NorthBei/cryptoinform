export const botName = process.env.NEXT_PUBLIC_BOT_NAME;
export const botUrl = `https://t.me/${botName}`;
export const webSiteUrl = 'https://cryptoinform.netlify.app';

const obj = {
  botName,
  botUrl,
  webSiteUrl,
};

export default obj;
